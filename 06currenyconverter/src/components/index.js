import InputBox from "./InputBox";

export {InputBox};

// better practise to make a index file in component and import all the components and export all, so that when we need component we only need to call one file for all components not all files seperately.